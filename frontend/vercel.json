{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "DISABLE_ESLINT_PLUGIN=true GENERATE_SOURCEMAP=false npm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
